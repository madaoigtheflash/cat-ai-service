# Cat-AI 小程序设计系统

## 氛围

轻松、柔软、少女感，但保持可信。使用“猫咪陪伴手账”而非重度游戏视觉：圆角卡片、贴纸式图标、轻微呼吸动画和即时反馈。健康警告必须清楚严肃。

## 色彩

- 页面底色：`#FFF7FA`
- 卡片：`#FFFFFF`
- 柔粉表面：`#FFF0F5`
- 主粉：`#FF6F91`
- 按下：`#F45179`
- 浅粉禁用：`#FFD4DF`
- 主文字：`#3E2D35`
- 正文：`#6F5A64`
- 次要文字：`#9D8490`
- 边框：`#F2DFE7`
- 淡紫陪衬：`#F3ECFF`
- 成功、警告、错误仅使用：`#3F8E75`、`#A66A20`、`#B94955`

主粉只承担 CTA、选中态和进度，不让所有卡片同时高饱和。

## 字体

字体栈：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif`。不加载远程字体。

| 用途 | 字号 | 字重 | 行高 |
|---|---:|---:|---:|
| 页面大标题 | `42–44rpx` | 700 | 1.35 |
| 分区标题 | `32–34rpx` | 700 | 1.4 |
| 卡片标题 | `28–30rpx` | 600 | 1.45 |
| 正文 | `26–28rpx` | 400 | 1.65 |
| 辅助文字 | `23–24rpx` | 400 | 1.55 |
| 微标签 | 最低 `21rpx` | 600 | 1.45 |

只使用 400、500、600、700。重要信息不得单行截断；只有重复性元数据可以省略号。

## 间距与尺寸

基础尺度：`8、12、16、24、32、40、48rpx`。

- 页面左右边距：`32rpx`
- 卡片内边距：`28–32rpx`
- 卡片间距：`20–24rpx`
- 标题与正文：`12–16rpx`
- 分区间距：`40–48rpx`
- 触控区域：至少 `88rpx`
- 内容底部：`calc(72rpx + env(safe-area-inset-bottom))`

避免依赖固定高度承载文字。按钮使用 `min-height + padding + display:flex`，输入框使用 `min-height`。

## 组件

- 卡片：白色、`28rpx` 圆角、1rpx 粉灰边框，阴影只用一层轻阴影。
- 主按钮：粉色渐变、白字、`28–30rpx`、600 字重，按下缩放至 0.98。
- 次按钮：柔粉底、深粉字。
- 输入框：柔粉白底、1rpx 边框、`22–24rpx` 内边距，文本允许自然增高。
- 标签：只放短文本；长文本改用普通段落。
- 游戏反馈：呼吸/漂浮动画、完成印章、进度条和 `+经验` 浮层；动画控制在 160–400ms，待机循环应舒缓。

## 防遮挡清单

1. 装饰层 `pointer-events:none` 且层级低于文字。
2. Flex 文本容器设置 `min-width:0`，长内容设置 `word-break:break-word`。
3. 不给标题、正文、错误、回答设置固定高度。
4. 按钮文字不使用与高度相同的固定 `line-height`。
5. 表单双列在空间不足时允许换成单列。
6. 在微信开发者工具中使用默认和大号系统字体各检查一次。
